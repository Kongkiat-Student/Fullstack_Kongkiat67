const furniture = ['Table', 'Chairs', 'Couch',];
const newlist = [];

// furniture.forEach((item) => newlist.push(item));
// console.log(newlist);

for (count = 0; count < furniture.length; count++) {
    newlist.push(furniture[count]);
    console.log(newlist);
}

